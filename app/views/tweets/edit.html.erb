<%= form_with(model: @tweet, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :status %>
    <%= form.text_field :status, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.submit "Update Tweet", class: "btn btn-primary" %>
  </div>
<% end %>
